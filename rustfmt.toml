# Stable rustfmt options only (nightly-only options break `cargo fmt --check` on stable).
edition = "2024"
style_edition = "2024"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
